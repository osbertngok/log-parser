// go generate ./...
// GENERATED BY THE COMMAND ABOVE; DO NOT EDIT

package models
import (
    "bytes"
    "fmt"
    "time"
    "strconv"
)

{{ .RecordClass }}

func (r *Record) ToCSV() string {
    var buffer bytes.Buffer

    {{ range .Table.Data }}
        {{ if eq .ValueType "float64" }}
            buffer.WriteString(fmt.Sprintf("\"%s,\"", strconv.FormatFloat(r{{ keyChainsToGoFields .KeyChains }}, 'f', 6, 64)))
        {{ end }}
        {{ if eq .ValueType "string" }}
            buffer.WriteString(fmt.Sprintf("\"%s\",", r{{ keyChainsToGoFields .KeyChains }}))
        {{ end }}
        {{ if eq .ValueType "bool" }}
            buffer.WriteString(fmt.Sprintf("%t,", r{{ keyChainsToGoFields .KeyChains }}))
        {{ end }}
    {{ end }}

    return buffer.String()
}